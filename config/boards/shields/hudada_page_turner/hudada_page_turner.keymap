#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>     // 引入蓝牙行为定义
#include <dt-bindings/zmk/reset.h> // 引入复位行为定义

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &kp A      &ht &bt BT_CLR &sys_reset
            >;
        };
    };
};