#include <behaviors.dtsi> // 这一次，我们包含了定义所有基础行为的文件
#include <dt-bindings/zmk/keys.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &kp A      &kp B
            >;
        };
    };
};